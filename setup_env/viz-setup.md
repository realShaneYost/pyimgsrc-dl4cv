brew install node          # if you don't have it
chmod +x start-viz.sh      # already done if you download mine, but just in case
./start-viz.sh             # installs deps on first run, then opens browser


./start-viz.sh
